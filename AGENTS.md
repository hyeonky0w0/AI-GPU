# LG Aimers 제구 성공 확률 예측 프로젝트

## 1. 프로젝트 목표

투구 직전까지 확인 가능한 정보만 사용해 각 투구의 `control_success=1` 확률을 예측한다. 라벨이 아니라 0~1 확률을 제출한다.

- 평가: `Score = max(0, 100000 * (1 - Brier / (r * (1-r))))`
- `Brier = mean((p-y)^2)`
- 평가 데이터의 `r`은 비공개이므로 로컬 BSS는 모델 선택용 근사치다.
- 기준 리더보드: RandomForest 549.51, 최초 CatBoost 474.37, 수정 CatBoost 690.69.
- 1차 목표는 690.69 재현 가능한 파이프라인을 만드는 것이고, 그 뒤에만 개선 실험을 한다.

## 2. 대회 실행 계약

제출 ZIP 최상위 구조는 정확히 다음과 같아야 한다.

```text
submit.zip
├── model/
├── script.py
└── requirements.txt
```

평가 서버가 `data/`와 `output/`을 추가한다. `script.py`는 반드시 다음 계약을 지킨다.

- 입력: `./data/test.csv`
- 출력: `./output/submission.csv`
- 출력 컬럼: `row_id,control_success`
- 출력 행 수와 `row_id` 순서는 `test.csv`와 정확히 일치
- 확률은 유한한 `[0,1]` 값
- 추론 중 인터넷 사용 및 외부 다운로드 금지
- Ubuntu 22.04.5, Python 3.11.15, 6 vCPU, RAM 28GB, NVIDIA L4 환경
- 패키지 설치 10분 이내, 245,789행 추론 10분 이내
- ZIP 10GB 이하, 압축 해제 후 32GB 이하
- ZIP 최상위에 추가 폴더나 래퍼 디렉터리, 노트북, 데이터, `.venv`, 캐시를 넣지 않는다.

공식 안내에 `open/` 표현이 섞여 있어도 현재 코드 제출 계약의 표준 입력은 `data/test.csv`다. 근거 없이 경로를 바꾸지 말고, 필요하면 호환 fallback만 별도로 구현한다.

## 3. 데이터와 누수 방지

- 원본 `data/*.csv`는 절대 수정하지 않는다.
- 타깃은 `control_success`, 식별자는 `row_id`다. `row_id`는 피처에서 제외한다.
- 학습 피처의 기준은 `test.csv`에 실제 존재하는 컬럼이다. 학습·검증·추론의 피처 목록과 순서를 저장하고 동일하게 적용한다.
- 현재 투구 이후에 알 수 있는 결과, 실제 공 위치, 판정, 사후 Trackman 값은 사용하지 않는다.
- 테스트 행끼리 누적·집계·빈도 계산, 테스트 전체 분포를 본 보정, 행 순서 기반 rolling/expanding은 금지한다.
- 운영진이 제공한 `asof_*`는 사용 가능하지만, 새 이력 피처는 반드시 각 투구 이전 정보만 사용해야 한다.
- `trackman_history.csv`는 메인 데이터와 1:1 키 조인되는 자료가 아니다. 초기에 전체 로드하거나 억지 조인하지 않는다. 사용 시 학습 때 누수 없이 집계하고, 추론용 결과는 `model/`에 저장한다.

## 4. 검증 원칙

랜덤 행 분할을 모델 선택 근거로 사용하지 않는다. 기본 검증은 시간 순서를 지키는 롤링 검증이다.

| 학습 | 검증 |
| --- | --- |
| 2019~2021 | 2022 |
| 2019~2022 | 2023 |
| 2019~2023 | 2024 |

- 각 검증연도보다 이전 시즌만 학습에 사용한다.
- 단일 2024 홀드아웃은 빠른 개발용으로만 허용하며, 최종 판단은 롤링 평균과 최악 폴드를 함께 본다.
- 기록 항목: train/validation 타깃률, Brier, deployable constant Brier, 근사 BSS, AUC, LogLoss, 예측 평균·편향·표준편차, best iteration, seed 평균±표준편차, 신규 범주율, 실행시간, 최대 메모리.
- BSS 계산의 기준 확률은 학습 구간 타깃 평균처럼 배포 시점에 알 수 있는 값이어야 한다. 검증 타깃 평균을 예측 상수로 쓰는 oracle 기준선과 구분한다.
- 모델 선택은 작은 단일 폴드 개선이 아니라 여러 폴드에서의 일관성과 최악 폴드 악화 여부를 기준으로 한다.

## 5. 확인된 사실과 미해결 가설

- 최초 CatBoost 474.37의 핵심 문제는 랜덤 분할, 학습·추론 피처 불일치 가능성, 반복 횟수 선택, ID-예측 매핑 및 제출 구조 불안정이었다.
- 시즌 홀드아웃, early stopping, 테스트 기준 피처 고정, 3-seed 평균, `row_id` 직접 대응으로 690.69까지 상승했다.
- 그러나 2023 폴드에서 성능이 거의 상수 수준으로 붕괴했다. 690점 모델이 강한 신호를 찾았다기보다 약한 패턴을 활용한 모델일 수 있다.
- 선수 ID 제거는 일부 폴드에서만 도움을 줬고, 팀 ID까지 제거하면 항상 좋아지지 않았다.
- `season` 제거 효과는 아직 공정하게 결론내리지 못했다. 유지/제거를 동일 조건에서 직접 비교한다.
- 범주형 처리를 끈 실험이 더 좋았다는 관찰이 있으므로 `native categorical`과 `numeric/as-is`를 독립 실험으로 비교한다. 한 결과를 일반 법칙으로 단정하지 않는다.
- `game_type` 의존도가 과도했던 결과가 있어, 연도별 빈도·범주별 타깃률을 확인하고 제거 실험을 한다.
- 분포 변화가 큰 피처 제거와 중복 피처 제거의 개선은 매우 작아 확정 개선안이 아니다.
- 평균 예측이 실제 성공률보다 약 2%p 높았던 폴드가 있다. 캘리브레이션은 OOF/과거 검증 예측만으로 학습하고 롤링 폴드에서 검증한다.
- 우선순위 가설: `game_type`, `season`, 범주형 처리 방식, 최근 시즌 가중치, 누수 없는 as-of 최근폼/커리어 차이, 확률 보정.

## 6. CatBoost 기준선

재현 기준은 다음과 같다. 값은 실험으로 재검증하되 한 번에 여러 축을 바꾸지 않는다.

- `CatBoostClassifier`
- seeds: `[42, 2026, 3407]`
- depth 7, learning rate 0.05, max iterations 2000
- loss: Logloss
- early stopping: 100
- 검증에서 seed별 best iteration을 얻고, 최종 전체 학습은 해당 반복 횟수를 고정
- seed별 확률을 단순 평균
- 모델은 `model/catboost_0.cbm` 등으로 저장하고, `model/config.json`에 피처 순서·범주형 목록·모델 파일명·모델 파라미터·보정 설정을 저장

GPU에서 Brier 평가가 CPU 계산으로 전환되거나 병목이 생길 수 있으므로 학습 지표는 Logloss를 사용해도 된다. 모델 선택용 Brier는 `predict_proba` 결과로 직접 계산한다.

## 7. 첨부 코드 감사 결과

`1차_CatBoost.ipynb`를 그대로 실행하거나 제출물로 신뢰하지 않는다. 확인된 결함:

1. `X_train`, `X_val`, `y_train`, `y_val`을 만드는 시즌 분할 셀이 없다.
2. 범주형 목록을 처음 만든 뒤 테스트 기준으로 피처를 다시 정의하여 목록 불일치 가능성이 있다.
3. 모델은 `catboost_ensemble_submission/model/catboost_0~2.cbm`에 저장하지만, `script.py`는 다른 폴더의 `model/catboost.cbm` 하나만 찾는다.
4. `config.json`의 `model_files`를 저장하지만 추론 코드가 이를 사용하지 않는다.
5. ZIP 검사는 `model/catboost.cbm`을 요구해 실제 저장 파일과 모순된다.
6. 노트북 설명의 랜덤 80:20 검증과 실제 코드가 일치하지 않는다.
7. 제출 `requirements.txt`에 평가 서버 기본 패키지인 numpy/pandas를 버전 없이 다시 설치하려는 부분은 충돌과 설치시간 위험이 있다.

기존 RF 저장소의 장점인 경로 검증, 출력 검사, 서버 레이아웃 스모크 테스트는 재사용하되, 누락 예측을 placeholder로 유지하는 방식은 금지한다. ID가 맞지 않으면 즉시 실패해야 한다.

## 8. 저장소와 실험 규칙

- 새 실험은 `experiments/NNN_short_name/`에 만들고 기존 실험·노트북·결과를 덮어쓰지 않는다.
- 각 실험에는 실행 코드, 설정, `RESULT.md`를 둔다.
- `RESULT.md`에는 관찰 사실, 해석/가설, 다음 판별 실험을 분리한다.
- 먼저 작은 smoke 모드로 파이프라인을 검증하고, 사용자 승인 없이 전체 학습을 시작하지 않는다.
- 전체 `trackman_history.csv` 로딩, 대용량 모델 학습, 패키지 설치, Git commit/push, 원격 저장소 설정은 명시적 요청 없이 수행하지 않는다.
- 현재 로컬 환경은 Windows 외장 SSD `E:\LG_Aimers`, Python 3.12.10 `.venv`, 브랜치 `agent/local-experiment`다. 평가 서버는 Python 3.11.15이므로 제출 호환성을 별도 점검한다.
- 답변, 코드 주석, 실험 기록은 한국어로 작성한다.
- 코드 변경 후 관련 테스트를 직접 실행하고, 실행하지 못한 검증은 했다고 쓰지 않는다.
- 사용자의 기존 변경을 보존하며 파괴적 Git 명령을 사용하지 않는다.

## 9. 제출 전 필수 검증

1. 저장한 모델을 새 프로세스에서 로드한다.
2. 5행 샘플과 대규모 모의 test에서 추론한다.
3. 입력 컬럼 누락·추가·순서 변경을 명시적으로 검사한다.
4. 출력 행 수, 컬럼명, `row_id` 순서/중복/결측, 확률 유한성·범위를 검사한다.
5. 평가 서버처럼 ZIP을 임시 디렉터리에 풀고 `data/`, `output/`을 붙여 `python script.py`를 실행한다.
6. 6개 스레드 제한 상태에서 245,789행 추론 시간과 메모리를 측정한다.
7. ZIP 내부 최상위가 오직 `model/`, `script.py`, `requirements.txt`인지 검사한다.
8. 오프라인 실행에서 네트워크 호출이나 런타임 다운로드가 없는지 확인한다.

## 10. 작업 보고 형식

작업 종료 시 반드시 다음을 짧게 보고한다.

- 생성·수정 파일
- 실행한 명령과 실제 검증 결과
- 모델/데이터 선택 근거
- 확인된 위험과 미검증 항목
- 다음 한 단계

